const WalletAvailability = Object.freeze({
    UNKNOWN: 'UNKNOWN',
    UNAVAILABLE: 'UNAVAILABLE',
    ELIGIBLE: 'ELIGIBLE',
    ACTIVE: 'ACTIVE',
    PAUSED: 'PAUSED',
});

export default WalletAvailability;
